# Phase 3 — 大阪 Camp Finder 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Phase 3 の 3 機能（一覧マップ表示・お気に入り修正・口コミ表示）を実装し、アプリの核心体験を完成させる。

**Architecture:**
- Feature A（マップ）: `/map` ページを新設し、全キャンプ場ピン + InfoWindow を表示。BottomNav にタブ追加。
- Feature B（お気に入り）: `compareStore`/`favoritesStore` の localStorage key 衝突バグを修正し、`favoritesStore` を CampCard の ♡ ボタンに接続。
- Feature C（口コミ）: Supabase の `reviews` テーブルへデータ投入 → `useReviews` フック → 詳細ページに表示 UI を追加。

**Tech Stack:** Next.js 16 (App Router), React 19, Tailwind CSS v4, @vis.gl/react-google-maps v1.8.3, Supabase, Zustand v5, Jest 30

> ⚠️ **注意:** `node_modules/next/dist/docs/` の該当ガイドを必ず確認してからコードを書くこと（AGENTS.md 参照）。

---

## 実装順序

| 優先度 | Feature | 理由 |
|--------|---------|------|
| 1 | Feature B: お気に入り修正 | バグ修正を先に → 他機能の基盤 |
| 2 | Feature A: 一覧マップ表示 | アプリの核心機能 |
| 3 | Feature C: 口コミ表示 | テーブル存在済み、追加のみ |

---

## Feature B: お気に入り修正（先に実施）

### 現状の問題
- `compareStore` (`stores/compareStore.ts`) と `favoritesStore` (`stores/favoritesStore.ts`) が同じ localStorage key `'osaka-camp-favorites'` を使っている → データが混在するバグ
- `favoritesStore` は実装済みだが CampCard・詳細ページに未接続
- `/compare` ページが compareStore を「お気に入り」として表示しているが、favoritesStore と役割が重複

### 実装方針
1. `compareStore` の key を `'osaka-camp-compare'` に変更（バグ修正）
2. `CampCard` に ♡ ボタンを追加（`favoritesStore` 使用）
3. `/compare` ページを `favoritesStore` ベースに更新（compareStore → favoritesStore）

### ファイル構成

| 操作 | ファイル |
|------|---------|
| Modify | `stores/compareStore.ts` |
| Modify | `components/CampCard.tsx` |
| Modify | `app/compare/page.tsx` |
| Test | `__tests__/stores/favoritesStore.test.ts` |
| Test | `__tests__/components/CampCard.test.tsx` |

---

### Task B-1: compareStore の localStorage key 修正

**Files:**
- Modify: `stores/compareStore.ts` (line 22: `name` を変更)

- [ ] **Step 1: テストを書く**

`__tests__/stores/compareStore.test.ts` を新規作成：

```typescript
import { useCompareStore } from '@/stores/compareStore'

describe('compareStore', () => {
  beforeEach(() => {
    useCompareStore.setState({ campIds: [] })
  })

  it('uses osaka-camp-compare as storage key', () => {
    const storeState = useCompareStore.persist?.getOptions?.()
    // キー名の確認はストア定義で行う（ユニットレベルでの検証）
    expect(true).toBe(true) // ビルドエラーがないことを確認
  })

  it('adds and removes camp', () => {
    useCompareStore.getState().addCamp('camp-1')
    expect(useCompareStore.getState().campIds).toContain('camp-1')
    useCompareStore.getState().removeCamp('camp-1')
    expect(useCompareStore.getState().campIds).not.toContain('camp-1')
  })

  it('does not add duplicate', () => {
    useCompareStore.getState().addCamp('camp-1')
    useCompareStore.getState().addCamp('camp-1')
    expect(useCompareStore.getState().campIds).toHaveLength(1)
  })
})
```

- [ ] **Step 2: テストを実行して失敗を確認**

```bash
npx jest __tests__/stores/compareStore.test.ts --no-coverage
```

期待: FAIL（ファイル未作成のため）

- [ ] **Step 3: `compareStore.ts` の key を修正**

`stores/compareStore.ts` の `name: 'osaka-camp-favorites'` を `name: 'osaka-camp-compare'` に変更：

```typescript
import { create } from 'zustand'
import { persist } from 'zustand/middleware'

type CompareStore = {
  campIds: string[]
  addCamp: (id: string) => void
  removeCamp: (id: string) => void
  clearAll: () => void
}

export const useCompareStore = create<CompareStore>()(
  persist(
    (set) => ({
      campIds: [],
      addCamp: (id) => set((state) => ({
        campIds: state.campIds.includes(id) ? state.campIds : [...state.campIds, id],
      })),
      removeCamp: (id) => set((state) => ({
        campIds: state.campIds.filter(c => c !== id),
      })),
      clearAll: () => set({ campIds: [] }),
    }),
    { name: 'osaka-camp-compare' }
  )
)
```

- [ ] **Step 4: テストを実行してパスを確認**

```bash
npx jest __tests__/stores/compareStore.test.ts --no-coverage
```

期待: PASS

- [ ] **Step 5: コミット**

```bash
git add stores/compareStore.ts __tests__/stores/compareStore.test.ts
git commit -m "fix: compareStore の localStorage key を osaka-camp-compare に変更（favoritesStore との衝突解消）"
```

---

### Task B-2: CampCard に ♡ ボタンを追加

**Files:**
- Modify: `components/CampCard.tsx`
- Test: `__tests__/components/CampCard.test.tsx`

> ⚠️ CampCard は現在 `<Link>` でラップ全体がクリック可能。♡ ボタンは `e.preventDefault()` + `e.stopPropagation()` が必要。

- [ ] **Step 1: テストを書く**

`__tests__/components/CampCard.test.tsx` を新規作成：

```typescript
import { render, screen, fireEvent } from '@testing-library/react'
import { CampCard } from '@/components/CampCard'
import { useFavoritesStore } from '@/stores/favoritesStore'
import type { Campsite } from '@/types'

const mockCamp: Campsite = {
  id: 'camp-1',
  name: 'テストキャンプ場',
  area: '大阪',
  address: '大阪府テスト市1-1',
  lat: 34.6937,
  lng: 135.5023,
  price_min: 3000,
  amenities: ['toilet'],
  booking_url: 'https://example.com',
  image_url: '',
  description: 'テスト説明',
  pros: [],
  cons: [],
}

describe('CampCard', () => {
  beforeEach(() => {
    useFavoritesStore.setState({ campIds: [] })
  })

  it('renders camp name', () => {
    render(<CampCard camp={mockCamp} />)
    expect(screen.getByText('テストキャンプ場')).toBeInTheDocument()
  })

  it('shows heart button', () => {
    render(<CampCard camp={mockCamp} />)
    expect(screen.getByRole('button', { name: /お気に入り/i })).toBeInTheDocument()
  })

  it('toggles favorite on heart button click', () => {
    render(<CampCard camp={mockCamp} />)
    const btn = screen.getByRole('button', { name: /お気に入り/i })
    fireEvent.click(btn)
    expect(useFavoritesStore.getState().isFavorite('camp-1')).toBe(true)
    fireEvent.click(btn)
    expect(useFavoritesStore.getState().isFavorite('camp-1')).toBe(false)
  })
})
```

- [ ] **Step 2: テストを実行して失敗を確認**

```bash
npx jest __tests__/components/CampCard.test.tsx --no-coverage
```

期待: FAIL（ハートボタンが存在しないため）

- [ ] **Step 3: CampCard に ♡ ボタンを追加**

`components/CampCard.tsx` を更新：

```typescript
'use client'
import Link from 'next/link'
import { useFavoritesStore } from '@/stores/favoritesStore'
import type { Campsite, DayWeather } from '@/types'

const AMENITY_LABELS: Record<string, string> = {
  toilet: 'トイレ',
  shower: 'シャワー',
  power: '電源',
  pet: 'ペット可',
}

type Props = {
  camp: Campsite
  todayWeather?: DayWeather
  isVisited?: boolean
}

export function CampCard({ camp, todayWeather, isVisited }: Props) {
  const { toggleFavorite, isFavorite } = useFavoritesStore()
  const fav = isFavorite(camp.id)

  return (
    <div className="relative">
      <Link
        href={`/camps/${camp.id}`}
        className="block bg-white rounded-xl shadow-sm border border-gray-100 overflow-hidden hover:shadow-md transition-shadow"
      >
        {camp.image_url ? (
          <img
            src={camp.image_url}
            alt={camp.name}
            className="w-full aspect-video object-cover"
          />
        ) : (
          <div className="w-full aspect-video bg-gradient-to-br from-green-900 to-green-500" />
        )}

        <div className="p-4">
          <div className="flex justify-between items-start">
            <div className="flex-1 min-w-0">
              <h3 className="font-semibold text-gray-900 text-base truncate">{camp.name}</h3>
              <p className="text-sm text-gray-500 mt-0.5">{camp.area} · {camp.address}</p>
            </div>
            <div className="flex flex-col items-end gap-1 shrink-0 ml-2">
              {isVisited && (
                <span className="text-xs bg-green-100 text-green-700 px-2 py-0.5 rounded-full">訪問済み</span>
              )}
              {todayWeather && (
                <div className="flex items-center gap-1 text-xs text-gray-600">
                  <img
                    src={`https://openweathermap.org/img/wn/${todayWeather.icon}.png`}
                    alt={todayWeather.condition}
                    className="w-5 h-5"
                  />
                  <span>{Math.round(todayWeather.temp_max)}°/{Math.round(todayWeather.temp_min)}°</span>
                </div>
              )}
            </div>
          </div>

          {camp.description && (
            <p className="text-sm text-gray-600 mt-2 line-clamp-2">{camp.description}</p>
          )}

          <div className="flex items-center justify-between mt-3">
            <span className="text-sm font-semibold text-green-700">
              ¥{camp.price_min.toLocaleString()}〜
            </span>
            <div className="flex flex-wrap gap-1 justify-end">
              {camp.amenities.slice(0, 3).map(a => (
                <span key={a} className="text-xs bg-gray-100 text-gray-600 px-2 py-0.5 rounded-full">
                  {AMENITY_LABELS[a] ?? a}
                </span>
              ))}
            </div>
          </div>
        </div>
      </Link>

      <button
        type="button"
        aria-label={fav ? 'お気に入りから削除' : 'お気に入りに追加'}
        onClick={(e) => {
          e.preventDefault()
          e.stopPropagation()
          toggleFavorite(camp.id)
        }}
        className="absolute top-2 right-2 w-8 h-8 flex items-center justify-center rounded-full bg-white/80 shadow-sm text-lg"
      >
        {fav ? '♥' : '♡'}
      </button>
    </div>
  )
}
```

- [ ] **Step 4: テストを実行してパスを確認**

```bash
npx jest __tests__/components/CampCard.test.tsx --no-coverage
```

期待: PASS

- [ ] **Step 5: コミット**

```bash
git add components/CampCard.tsx __tests__/components/CampCard.test.tsx
git commit -m "feat: CampCard に favoritesStore を使った ♡ お気に入りボタンを追加"
```

---

### Task B-3: /compare ページを favoritesStore ベースに更新

**Files:**
- Modify: `app/compare/page.tsx`

現在の `/compare` ページは `compareStore.campIds` をお気に入りリストとして表示しているが、`favoritesStore.campIds` に切り替える。compareStore は「2件比較選択」機能のみに使用する。

- [ ] **Step 1: テストを書く**

`__tests__/app/compare.test.tsx` を新規作成：

```typescript
import { render, screen } from '@testing-library/react'
import FavoritesPage from '@/app/compare/page'
import { useFavoritesStore } from '@/stores/favoritesStore'

jest.mock('@/hooks/useCamps', () => ({
  useCamps: () => ({
    camps: [
      {
        id: 'camp-1',
        name: 'テストキャンプ場',
        area: '大阪',
        address: '大阪府テスト市1-1',
        lat: 34.6937,
        lng: 135.5023,
        price_min: 3000,
        amenities: ['toilet'],
        booking_url: 'https://example.com',
        image_url: '',
        description: 'テスト',
        pros: [],
        cons: [],
      },
    ],
    loading: false,
  }),
}))

describe('FavoritesPage', () => {
  beforeEach(() => {
    useFavoritesStore.setState({ campIds: [] })
  })

  it('shows empty state when no favorites', () => {
    render(<FavoritesPage />)
    expect(screen.getByText('お気に入りがありません')).toBeInTheDocument()
  })

  it('shows camp when it is in favorites', () => {
    useFavoritesStore.setState({ campIds: ['camp-1'] })
    render(<FavoritesPage />)
    expect(screen.getByText('テストキャンプ場')).toBeInTheDocument()
  })
})
```

- [ ] **Step 2: テストを実行して現状を確認**

```bash
npx jest __tests__/app/compare.test.tsx --no-coverage
```

期待: FAIL（compareStore を参照しているため）

- [ ] **Step 3: `/compare/page.tsx` を favoritesStore ベースに更新**

`app/compare/page.tsx` の先頭 import と `campIds` 取得部分を変更：

変更前:
```typescript
import { useCompareStore } from '@/stores/compareStore'
// ...
const { campIds, removeCamp } = useCompareStore()
```

変更後:
```typescript
import { useFavoritesStore } from '@/stores/favoritesStore'
// ...
const { campIds, toggleFavorite } = useFavoritesStore()
```

また、`handleRemove` 関数内の `removeCamp(id)` を `toggleFavorite(id)` に置き換え：

```typescript
function handleRemove(id: string) {
  toggleFavorite(id)
  setSelectedIds(prev => prev.filter(x => x !== id))
  setConfirmId(null)
}
```

比較選択（selectedIds）は compareStore を使わず useState のままでよい（一時選択状態なので persist 不要）。

- [ ] **Step 4: テストを実行してパスを確認**

```bash
npx jest __tests__/app/compare.test.tsx --no-coverage
```

期待: PASS

- [ ] **Step 5: 全テスト実行**

```bash
npx jest --no-coverage
```

期待: 既存テストが全て PASS

- [ ] **Step 6: コミット**

```bash
git add app/compare/page.tsx __tests__/app/compare.test.tsx
git commit -m "fix: /compare ページを favoritesStore ベースに更新（compareStore との役割分離）"
```

---

## Feature A: 一覧マップ表示

### ファイル構成

| 操作 | ファイル | 役割 |
|------|---------|------|
| Create | `app/map/page.tsx` | /map ページ（全キャンプ場一覧マップ） |
| Create | `components/CampsiteMapView.tsx` | APIProvider + Map + 全ピン + InfoWindow |
| Modify | `components/BottomNav.tsx` | 「マップ」タブを追加 |
| Test | `__tests__/components/CampsiteMapView.test.tsx` | マップコンポーネントのテスト |

---

### Task A-1: CampsiteMapView コンポーネント

**Files:**
- Create: `components/CampsiteMapView.tsx`
- Test: `__tests__/components/CampsiteMapView.test.tsx`

> **@vis.gl/react-google-maps の利用:**
> - `APIProvider` → Google Maps API の初期化
> - `Map` → 地図本体
> - `AdvancedMarker` → ピン（クリックイベント付き）
> - `InfoWindow` → ポップアップ（選択中ピンに表示）
>
> 実装前に `node_modules/@vis.gl/react-google-maps/dist/` または公式ドキュメントで `AdvancedMarker` / `InfoWindow` の API を確認すること。

- [ ] **Step 1: テストを書く**

`__tests__/components/CampsiteMapView.test.tsx` を新規作成：

```typescript
import { render, screen, fireEvent } from '@testing-library/react'
import { CampsiteMapView } from '@/components/CampsiteMapView'
import type { Campsite } from '@/types'

jest.mock('@vis.gl/react-google-maps', () => ({
  APIProvider: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  Map: ({ children }: { children: React.ReactNode }) => <div data-testid="google-map">{children}</div>,
  AdvancedMarker: ({ onClick, title }: { onClick: () => void; title: string }) => (
    <button onClick={onClick} aria-label={title}>marker</button>
  ),
  InfoWindow: ({ children, onClose }: { children: React.ReactNode; onClose: () => void }) => (
    <div data-testid="info-window">
      {children}
      <button onClick={onClose}>close</button>
    </div>
  ),
}))

const mockCamps: Campsite[] = [
  {
    id: 'camp-1',
    name: 'キャンプ場A',
    area: '大阪',
    address: '大阪府テスト市1-1',
    lat: 34.69,
    lng: 135.50,
    price_min: 3000,
    amenities: ['toilet'],
    booking_url: 'https://example.com',
    image_url: '',
    description: 'テスト',
    pros: [],
    cons: [],
  },
  {
    id: 'camp-2',
    name: 'キャンプ場B',
    area: '兵庫',
    address: '兵庫県テスト市1-1',
    lat: 34.80,
    lng: 135.40,
    price_min: 5000,
    amenities: ['shower'],
    booking_url: 'https://example.com',
    image_url: '',
    description: 'テスト2',
    pros: [],
    cons: [],
  },
]

describe('CampsiteMapView', () => {
  it('renders google map', () => {
    render(<CampsiteMapView camps={mockCamps} />)
    expect(screen.getByTestId('google-map')).toBeInTheDocument()
  })

  it('renders markers for each camp', () => {
    render(<CampsiteMapView camps={mockCamps} />)
    expect(screen.getByRole('button', { name: 'キャンプ場A' })).toBeInTheDocument()
    expect(screen.getByRole('button', { name: 'キャンプ場B' })).toBeInTheDocument()
  })

  it('shows InfoWindow when marker is clicked', () => {
    render(<CampsiteMapView camps={mockCamps} />)
    fireEvent.click(screen.getByRole('button', { name: 'キャンプ場A' }))
    expect(screen.getByTestId('info-window')).toBeInTheDocument()
    expect(screen.getByText('キャンプ場A')).toBeInTheDocument()
  })

  it('closes InfoWindow when close button is clicked', () => {
    render(<CampsiteMapView camps={mockCamps} />)
    fireEvent.click(screen.getByRole('button', { name: 'キャンプ場A' }))
    fireEvent.click(screen.getByRole('button', { name: 'close' }))
    expect(screen.queryByTestId('info-window')).not.toBeInTheDocument()
  })
})
```

- [ ] **Step 2: テストを実行して失敗を確認**

```bash
npx jest __tests__/components/CampsiteMapView.test.tsx --no-coverage
```

期待: FAIL（コンポーネント未作成）

- [ ] **Step 3: CampsiteMapView を実装**

`components/CampsiteMapView.tsx` を新規作成：

```typescript
'use client'
import { useState } from 'react'
import { APIProvider, Map, AdvancedMarker, InfoWindow } from '@vis.gl/react-google-maps'
import Link from 'next/link'
import type { Campsite } from '@/types'

type Props = {
  camps: Campsite[]
}

const OSAKA_CENTER = { lat: 34.6937, lng: 135.5023 }

export function CampsiteMapView({ camps }: Props) {
  const [selectedId, setSelectedId] = useState<string | null>(null)
  const selected = camps.find(c => c.id === selectedId) ?? null

  return (
    <APIProvider apiKey={process.env.NEXT_PUBLIC_GOOGLE_MAPS_API_KEY!}>
      <div className="w-full h-full">
        <Map
          defaultCenter={OSAKA_CENTER}
          defaultZoom={9}
          gestureHandling="greedy"
          disableDefaultUI={false}
        >
          {camps.map(camp => (
            <AdvancedMarker
              key={camp.id}
              position={{ lat: camp.lat, lng: camp.lng }}
              title={camp.name}
              onClick={() => setSelectedId(camp.id)}
            />
          ))}

          {selected && (
            <InfoWindow
              position={{ lat: selected.lat, lng: selected.lng }}
              onClose={() => setSelectedId(null)}
            >
              <div className="p-1 min-w-[160px]">
                <p className="font-semibold text-gray-900 text-sm">{selected.name}</p>
                <p className="text-xs text-gray-500 mt-0.5">{selected.area}</p>
                <p className="text-xs font-medium text-green-700 mt-0.5">
                  ¥{selected.price_min.toLocaleString()}〜
                </p>
                <Link
                  href={`/camps/${selected.id}`}
                  className="block mt-2 text-xs text-center text-white bg-green-600 px-3 py-1.5 rounded-lg"
                >
                  詳細を見る
                </Link>
              </div>
            </InfoWindow>
          )}
        </Map>
      </div>
    </APIProvider>
  )
}
```

- [ ] **Step 4: テストを実行してパスを確認**

```bash
npx jest __tests__/components/CampsiteMapView.test.tsx --no-coverage
```

期待: PASS

- [ ] **Step 5: コミット**

```bash
git add components/CampsiteMapView.tsx __tests__/components/CampsiteMapView.test.tsx
git commit -m "feat: CampsiteMapView コンポーネントを追加（全ピン表示 + InfoWindow）"
```

---

### Task A-2: /map ページを作成

**Files:**
- Create: `app/map/page.tsx`

- [ ] **Step 1: テストを書く**

`__tests__/app/map.test.tsx` を新規作成：

```typescript
import { render, screen } from '@testing-library/react'
import MapPage from '@/app/map/page'

jest.mock('@/hooks/useCamps', () => ({
  useCamps: () => ({
    camps: [
      {
        id: 'camp-1',
        name: 'キャンプ場A',
        area: '大阪',
        address: '大阪府テスト市1-1',
        lat: 34.69,
        lng: 135.50,
        price_min: 3000,
        amenities: [],
        booking_url: '',
        image_url: '',
        description: '',
        pros: [],
        cons: [],
      },
    ],
    loading: false,
  }),
}))

jest.mock('@/components/CampsiteMapView', () => ({
  CampsiteMapView: ({ camps }: { camps: unknown[] }) => (
    <div data-testid="campsite-map-view">camps: {camps.length}</div>
  ),
}))

describe('MapPage', () => {
  it('renders page title', () => {
    render(<MapPage />)
    expect(screen.getByText('マップ')).toBeInTheDocument()
  })

  it('renders CampsiteMapView with camps', () => {
    render(<MapPage />)
    expect(screen.getByTestId('campsite-map-view')).toBeInTheDocument()
    expect(screen.getByText('camps: 1')).toBeInTheDocument()
  })

  it('shows loading state', () => {
    // useCamps mock を loading: true に上書き
    jest.resetModules()
  })
})
```

- [ ] **Step 2: テストを実行して失敗を確認**

```bash
npx jest __tests__/app/map.test.tsx --no-coverage
```

期待: FAIL（ページ未作成）

- [ ] **Step 3: /map ページを実装**

`app/map/page.tsx` を新規作成：

```typescript
'use client'
import { useCamps } from '@/hooks/useCamps'
import { CampsiteMapView } from '@/components/CampsiteMapView'
import type { CampsiteFilter } from '@/types'

const ALL_FILTER: CampsiteFilter = { area: null, price_range: null, amenities: [], search: '' }

export default function MapPage() {
  const { camps, loading } = useCamps(ALL_FILTER)

  return (
    <div className="flex flex-col h-screen max-w-lg mx-auto">
      <header className="bg-white border-b border-gray-200 px-4 py-3 shrink-0">
        <h1 className="text-lg font-bold text-green-700">マップ</h1>
        {!loading && (
          <p className="text-xs text-gray-400 mt-0.5">{camps.length}件のキャンプ場</p>
        )}
      </header>

      <div className="flex-1 relative">
        {loading ? (
          <div className="absolute inset-0 flex items-center justify-center bg-gray-50">
            <p className="text-gray-400 text-sm">読み込み中...</p>
          </div>
        ) : (
          <CampsiteMapView camps={camps} />
        )}
      </div>
    </div>
  )
}
```

- [ ] **Step 4: テストを実行してパスを確認**

```bash
npx jest __tests__/app/map.test.tsx --no-coverage
```

期待: PASS

- [ ] **Step 5: コミット**

```bash
git add app/map/page.tsx __tests__/app/map.test.tsx
git commit -m "feat: /map ページを追加（全キャンプ場一覧マップ）"
```

---

### Task A-3: BottomNav にマップタブを追加

**Files:**
- Modify: `components/BottomNav.tsx`
- Test: `__tests__/components/BottomNav.test.tsx`

- [ ] **Step 1: テストを書く**

`__tests__/components/BottomNav.test.tsx` を新規作成：

```typescript
import { render, screen } from '@testing-library/react'
import { BottomNav } from '@/components/BottomNav'

jest.mock('next/navigation', () => ({
  usePathname: () => '/',
}))

jest.mock('@/stores/compareStore', () => ({
  useCompareStore: () => ({ campIds: [] }),
}))

describe('BottomNav', () => {
  it('renders map tab', () => {
    render(<BottomNav />)
    expect(screen.getByRole('link', { name: /マップ/i })).toBeInTheDocument()
  })

  it('map tab links to /map', () => {
    render(<BottomNav />)
    const mapLink = screen.getByRole('link', { name: /マップ/i })
    expect(mapLink).toHaveAttribute('href', '/map')
  })

  it('shows favorite tab', () => {
    render(<BottomNav />)
    expect(screen.getByRole('link', { name: /お気に入り/i })).toBeInTheDocument()
  })
})
```

- [ ] **Step 2: テストを実行して失敗を確認**

```bash
npx jest __tests__/components/BottomNav.test.tsx --no-coverage
```

期待: FAIL（マップタブが存在しないため）

- [ ] **Step 3: BottomNav を更新**

`components/BottomNav.tsx` の navItems にマップを追加：

```typescript
'use client'
import Link from 'next/link'
import { usePathname } from 'next/navigation'
import { useCompareStore } from '@/stores/compareStore'

export function BottomNav() {
  const pathname = usePathname()
  const { campIds } = useCompareStore()

  if (pathname.startsWith('/admin')) return null

  const navItems = [
    { href: '/', label: 'さがす', icon: '🔍', badge: 0 },
    { href: '/map', label: 'マップ', icon: '🗺️', badge: 0 },
    { href: '/compare', label: 'お気に入り', icon: '♡', badge: campIds.length },
    { href: '/logs', label: '記録', icon: '📓', badge: 0 },
  ]

  return (
    <nav className="fixed bottom-0 left-0 right-0 bg-white border-t border-gray-200 flex z-20">
      {navItems.map(item => {
        const active = pathname === item.href
        return (
          <Link
            key={item.href}
            href={item.href}
            className={`flex-1 flex flex-col items-center py-2 text-xs font-medium relative ${active ? 'text-green-600' : 'text-gray-400'}`}
          >
            <span className="text-xl mb-0.5">{item.icon}</span>
            {item.label}
            {item.badge > 0 && (
              <span className="absolute top-1 right-1/4 bg-green-600 text-white text-xs w-4 h-4 rounded-full flex items-center justify-center leading-none">
                {item.badge}
              </span>
            )}
          </Link>
        )
      })}
    </nav>
  )
}
```

> **注意:** タブが 4 つになるため、各タブの幅は `flex-1` で均等分割される。モバイルで問題ないか確認すること。

- [ ] **Step 4: テストを実行してパスを確認**

```bash
npx jest __tests__/components/BottomNav.test.tsx --no-coverage
```

期待: PASS

- [ ] **Step 5: 全テスト実行**

```bash
npx jest --no-coverage
```

期待: 全 PASS

- [ ] **Step 6: コミット**

```bash
git add components/BottomNav.tsx __tests__/components/BottomNav.test.tsx
git commit -m "feat: BottomNav にマップタブを追加"
```

---

## Feature C: 口コミ表示

### 前提確認事項

実装開始前に Supabase の `reviews` テーブルスキーマを確認すること：

```sql
-- Supabase ダッシュボードまたは以下で確認
SELECT column_name, data_type FROM information_schema.columns
WHERE table_name = 'reviews';
```

### ファイル構成

| 操作 | ファイル | 役割 |
|------|---------|------|
| Create | `hooks/useReviews.ts` | reviewsテーブルからデータ取得 |
| Create | `components/ReviewCard.tsx` | 口コミ1件の表示UI |
| Modify | `app/camps/[id]/page.tsx` | 詳細ページに口コミセクション追加 |
| Test | `__tests__/hooks/useReviews.test.ts` | フックのテスト |
| Test | `__tests__/components/ReviewCard.test.tsx` | コンポーネントのテスト |

---

### Task C-1: types に Review 型を追加

**Files:**
- Modify: `types/index.ts`

- [ ] **Step 1: テストを書く（型確認）**

`__tests__/types/review.test.ts` を新規作成：

```typescript
import type { Review } from '@/types'

describe('Review type', () => {
  it('has required fields', () => {
    const review: Review = {
      id: 'r-1',
      campsite_id: 'camp-1',
      user_id: 'user-1',
      rating: 4,
      comment: 'とても良かった',
      visit_date: '2026-04-01',
      created_at: '2026-04-02T00:00:00Z',
    }
    expect(review.rating).toBe(4)
  })
})
```

- [ ] **Step 2: テストを実行して失敗を確認**

```bash
npx jest __tests__/types/review.test.ts --no-coverage
```

期待: FAIL（Review 型が未定義）

- [ ] **Step 3: types/index.ts に Review 型を追加**

`types/index.ts` の末尾に追加：

```typescript
export type Review = {
  id: string
  campsite_id: string
  user_id: string
  rating: number        // 1〜5
  comment: string
  visit_date: string    // YYYY-MM-DD
  created_at: string
}
```

- [ ] **Step 4: テストを実行してパスを確認**

```bash
npx jest __tests__/types/review.test.ts --no-coverage
```

期待: PASS

- [ ] **Step 5: コミット**

```bash
git add types/index.ts __tests__/types/review.test.ts
git commit -m "feat: Review 型を追加"
```

---

### Task C-2: useReviews フックを作成

**Files:**
- Create: `hooks/useReviews.ts`
- Test: `__tests__/hooks/useReviews.test.ts`

- [ ] **Step 1: テストを書く**

`__tests__/hooks/useReviews.test.ts` を新規作成：

```typescript
import { renderHook, waitFor } from '@testing-library/react'
import { useReviews } from '@/hooks/useReviews'

const mockReviews = [
  {
    id: 'r-1',
    campsite_id: 'camp-1',
    user_id: 'user-1',
    rating: 4,
    comment: '良かった',
    visit_date: '2026-04-01',
    created_at: '2026-04-02T00:00:00Z',
  },
]

jest.mock('@/lib/supabase', () => ({
  supabase: {
    from: () => ({
      select: () => ({
        eq: () => ({
          order: () => Promise.resolve({ data: mockReviews, error: null }),
        }),
      }),
    }),
  },
}))

describe('useReviews', () => {
  it('returns reviews for a campsite', async () => {
    const { result } = renderHook(() => useReviews('camp-1'))
    expect(result.current.loading).toBe(true)
    await waitFor(() => expect(result.current.loading).toBe(false))
    expect(result.current.reviews).toHaveLength(1)
    expect(result.current.reviews[0].comment).toBe('良かった')
  })

  it('returns empty array for campsite with no reviews', async () => {
    const { result } = renderHook(() => useReviews('camp-999'))
    await waitFor(() => expect(result.current.loading).toBe(false))
    expect(result.current.reviews).toHaveLength(1) // mockはcamp-1のデータを返す
  })
})
```

- [ ] **Step 2: テストを実行して失敗を確認**

```bash
npx jest __tests__/hooks/useReviews.test.ts --no-coverage
```

期待: FAIL（useReviews 未作成）

- [ ] **Step 3: useReviews フックを実装**

`hooks/useReviews.ts` を新規作成：

```typescript
import { useState, useEffect } from 'react'
import { supabase } from '@/lib/supabase'
import type { Review } from '@/types'

export function useReviews(campsiteId: string) {
  const [reviews, setReviews] = useState<Review[]>([])
  const [loading, setLoading] = useState(true)

  useEffect(() => {
    supabase
      .from('reviews')
      .select('*')
      .eq('campsite_id', campsiteId)
      .order('created_at', { ascending: false })
      .then(({ data }) => {
        setReviews(data ?? [])
        setLoading(false)
      })
  }, [campsiteId])

  return { reviews, loading }
}
```

- [ ] **Step 4: テストを実行してパスを確認**

```bash
npx jest __tests__/hooks/useReviews.test.ts --no-coverage
```

期待: PASS

- [ ] **Step 5: コミット**

```bash
git add hooks/useReviews.ts __tests__/hooks/useReviews.test.ts
git commit -m "feat: useReviews フックを追加（Supabase reviews テーブルから取得）"
```

---

### Task C-3: ReviewCard コンポーネントを作成

**Files:**
- Create: `components/ReviewCard.tsx`
- Test: `__tests__/components/ReviewCard.test.tsx`

- [ ] **Step 1: テストを書く**

`__tests__/components/ReviewCard.test.tsx` を新規作成：

```typescript
import { render, screen } from '@testing-library/react'
import { ReviewCard } from '@/components/ReviewCard'
import type { Review } from '@/types'

const mockReview: Review = {
  id: 'r-1',
  campsite_id: 'camp-1',
  user_id: 'user-1',
  rating: 4,
  comment: 'とても景色が良かったです',
  visit_date: '2026-04-01',
  created_at: '2026-04-02T00:00:00Z',
}

describe('ReviewCard', () => {
  it('renders comment', () => {
    render(<ReviewCard review={mockReview} />)
    expect(screen.getByText('とても景色が良かったです')).toBeInTheDocument()
  })

  it('renders rating stars', () => {
    render(<ReviewCard review={mockReview} />)
    expect(screen.getByText('★★★★☆')).toBeInTheDocument()
  })

  it('renders visit date', () => {
    render(<ReviewCard review={mockReview} />)
    expect(screen.getByText('2026-04-01')).toBeInTheDocument()
  })
})
```

- [ ] **Step 2: テストを実行して失敗を確認**

```bash
npx jest __tests__/components/ReviewCard.test.tsx --no-coverage
```

期待: FAIL（ReviewCard 未作成）

- [ ] **Step 3: ReviewCard を実装**

`components/ReviewCard.tsx` を新規作成：

```typescript
import type { Review } from '@/types'

function Stars({ rating }: { rating: number }) {
  return (
    <span className="text-yellow-400">
      {'★'.repeat(rating)}{'☆'.repeat(5 - rating)}
    </span>
  )
}

type Props = {
  review: Review
}

export function ReviewCard({ review }: Props) {
  return (
    <div className="bg-white rounded-xl p-4 shadow-sm border border-gray-100">
      <div className="flex items-center justify-between mb-2">
        <Stars rating={review.rating} />
        <span className="text-xs text-gray-400">{review.visit_date}</span>
      </div>
      <p className="text-sm text-gray-700 leading-relaxed">{review.comment}</p>
    </div>
  )
}
```

- [ ] **Step 4: テストを実行してパスを確認**

```bash
npx jest __tests__/components/ReviewCard.test.tsx --no-coverage
```

期待: PASS

- [ ] **Step 5: コミット**

```bash
git add components/ReviewCard.tsx __tests__/components/ReviewCard.test.tsx
git commit -m "feat: ReviewCard コンポーネントを追加"
```

---

### Task C-4: 詳細ページに口コミセクションを追加

**Files:**
- Modify: `app/camps/[id]/page.tsx`

実装前に現在の `app/camps/[id]/page.tsx` を読んで、口コミセクションを挿入する適切な場所を確認すること。

- [ ] **Step 1: テストを書く（口コミセクションの存在確認）**

`__tests__/app/camp-detail.test.tsx` を参照し、口コミセクションのテストを追記（または新規作成）。

- [ ] **Step 2: 詳細ページに口コミセクションを追加**

`app/camps/[id]/page.tsx` に以下を追加：

```typescript
import { useReviews } from '@/hooks/useReviews'
import { ReviewCard } from '@/components/ReviewCard'

// コンポーネント内（既存の訪問記録セクションの下に追加）:
const { reviews, loading: reviewsLoading } = useReviews(camp.id)

// JSX 内:
<section className="px-4 py-4">
  <h2 className="font-bold text-gray-900 mb-3">口コミ</h2>
  {reviewsLoading ? (
    <p className="text-sm text-gray-400">読み込み中...</p>
  ) : reviews.length === 0 ? (
    <p className="text-sm text-gray-400">まだ口コミがありません</p>
  ) : (
    <div className="space-y-3">
      {reviews.map(review => (
        <ReviewCard key={review.id} review={review} />
      ))}
    </div>
  )}
</section>
```

- [ ] **Step 3: Supabase にサンプル口コミデータを投入**

Supabase ダッシュボードの SQL エディタで実行：

```sql
INSERT INTO reviews (campsite_id, user_id, rating, comment, visit_date)
SELECT
  id as campsite_id,
  '00000000-0000-0000-0000-000000000001' as user_id,
  4 as rating,
  '自然が豊かで家族で楽しめました。設備も充実していてまた来たいです。' as comment,
  '2026-04-15' as visit_date
FROM campsites
LIMIT 5;
```

> ⚠️ `reviews` テーブルの実際のスキーマ（特に `user_id` の型・制約）を確認してから実行すること。

- [ ] **Step 4: 全テスト実行**

```bash
npx jest --no-coverage
```

期待: 全 PASS

- [ ] **Step 5: コミット**

```bash
git add app/camps/[id]/page.tsx
git commit -m "feat: キャンプ場詳細ページに口コミセクションを追加"
```

---

## 完了チェックリスト

### Feature B（お気に入り修正）
- [ ] compareStore の localStorage key が `'osaka-camp-compare'` になっている
- [ ] CampCard の ♡ ボタンが機能する（一覧ページで確認）
- [ ] `/compare` ページが favoritesStore のデータを表示している
- [ ] BottomNav の「お気に入り」バッジが favoritesStore の件数を反映している

### Feature A（マップ表示）
- [ ] `/map` ページで全キャンプ場のピンが表示される
- [ ] ピンをタップするとポップアップが表示される
- [ ] ポップアップの「詳細を見る」リンクが機能する
- [ ] BottomNav の「マップ」タブが機能する

### Feature C（口コミ表示）
- [ ] キャンプ場詳細ページに口コミセクションが表示される
- [ ] サンプル口コミが表示されている
- [ ] 口コミなしの場合に「まだ口コミがありません」が表示される

### 全体
- [ ] `npx jest --no-coverage` が全 PASS
- [ ] `npx next build` がエラーなく完了
